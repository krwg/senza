# Senza 1.2.0 Dolce

**Dolce** — soft, polished, and ready for everyday listening. Milestone focus: foundation hardening (lint, CI, IPC guards, state validation) before larger modularization work.

## Highlights

- **Quality gates** — ESLint + GitHub Actions CI (lint / test / build) on every PR
- **Safer IPC** — file operations constrained to the music library root
- **Safer state** — renderer save-state payloads validated before disk write
- **Release notes** — GitHub Releases pick the matching `docs/release-notes-v*-*.md` for the tag

### Notes

This file is the release body source for tag `v1.2.0`. Expand with shipped features before cutting the Dolce release.
